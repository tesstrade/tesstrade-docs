{
  "name": "tesstrade-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run docs:build",
    "docs:build": "vitepress build && npx pagefind --site .vitepress/dist",
    "dev": "vitepress dev --host 0.0.0.0",
    "preview": "vitepress preview --host 0.0.0.0"
  },
  "devDependencies": {
    "@pagefind/default-ui": "^1.5.2",
    "pagefind": "^1.5.2",
    "vitepress": "1.6.4"
  }
}
